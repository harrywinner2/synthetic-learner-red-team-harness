# Build Log — Synthetic Learner Red Team Harness (Nerdy / Varsity Tutors)

One line per meaningful decision or phase transition. Read this instead of being interrupted.

## 2026-06-15
- **Intake** — PRD = `tmp1.pdf` (Gauntlet Challenger: Synthetic Learner Red Team Harness, contact harrison.glenn@varsitytutors.com). Client = Nerdy / Varsity Tutors.
- **Preflight** — git ✅, gh (harrywinner2) ✅, node v20 ✅, python 3.11 ✅, docker ✅, ffmpeg ✅. Deploy CLIs authed: Railway, Vercel, Wrangler. No browser MCP → installing Playwright locally as fallback.
- **Decision: deploy target = Railway.** Rationale: this is a Python/FastAPI backend that runs long multi-turn LLM simulations and needs a persistent DB (learner state + interaction log). Railway = one always-on service + Postgres, no serverless timeout. (Demo DB defaults to SQLite for portability; Postgres optional via DATABASE_URL.)
- **Decision: browser tooling = local Playwright + Chromium** for style capture, live-verify, demo recording.
- **Decision: sim scope = hybrid.** Ship a clearly-badged *cached* baseline↔improved comparison (instant, free to view) PLUS a prominent "Force live run" control that fires real OpenAI calls. Cached status is shown in the UI and called out in narration — honesty is the product's whole thesis.
- **Decision: subject under test = Algebra I, linear equations & slope.** Rationale: dense, well-documented misconception literature (reversal error, slope-as-"number-in-front-of-x", rate vs intercept, equality-as-operator) and squarely in Varsity Tutors' K-12 / test-prep territory.
- **Decision: frontend = polished single-page app in vanilla HTML/CSS/JS with hand-rolled SVG charts** (no CDN dependency, so the mockup opens offline and the deployed app renders identically), served as static by FastAPI. One deploy, full control over the "expressive" look.
- **Phase 1 done** — `docs/PRD-expanded.md` written: product POV, 8 personas (6 trained + 2 held-out), hidden-state oracle, pre/post/transfer instrument, metrics+counter-metrics, recursive loop, data model, AI surface, acceptance criteria.
- **Phase 2 done** — Playwright captured real tokens from varsitytutors.com + nerdy.com → `design/style-guide.md`. Brand: Poppins/Karla, deep indigo #20205F, electric indigo #432DD7, lavender #EFEDFF, magenta #FB43DA, Nerdy cyan #17E2EA, pill buttons. Adapted into a dark "command-center" theme.
- **Phase 3 done** — `ui/mockup.html` single self-contained file, all 8 screens, hand-rolled SVG radar/heatmap/bars (no CDN), real coherent data. Verified in Playwright: all screens render, zero console errors. Screenshots in `design/mockup-*.png`.
- **Phase 4 started** — architecture: FastAPI backend serving static frontend; SQLite (Postgres-ready); domain modules (personas, BKT knowledge model, learner/tutor agents, engine, classifier, evaluator, diagnostician, compare).
- **Phase 5 done** — demo video `demo/demo.mp4`: OpenAI TTS narration over Playwright screen-capture of the live site, ffmpeg-assembled. v1 was 3.7min; per user feedback expanded to **v2 = 7.5min / 14 beats** covering architecture (LLM-agent + hidden-BKT-oracle), the engine, evaluation + counter-metrics, held-out/independent-judge trust, the recursive loop, usability (cached vs live + BYO key), scaling/engineering (concurrency, caps, model swap, SQLite→Postgres, one Docker service), the codebase, and limitations. 1080p, h264+aac. Tooling: `demo/script.md`, `demo/capture_clips.mjs`.
- **Phase 4 SHIPPED** — repo: https://github.com/harrywinner2/synthetic-learner-red-team-harness (public). Live: https://web-production-89f52.up.railway.app (Railway, Dockerfile build, OPENAI_API_KEY set as a deploy secret via stdin — never in repo/argv). 8 logical commits. 19 tests pass. Cached fixture is a real recorded run (REAL_PROGRESS; learning 0.02→0.32, transfer 0.09→0.41, answer-giving 0.62→0.02, misconception 0.08→0.75). Live-verified in Playwright: all 8 screens, zero console errors; a real "Force live run" executed OpenAI on the deployed app and persisted run_id 1. First deploy 502'd (railway.json startCommand didn't shell-expand $PORT) → fixed by deferring to the Dockerfile CMD.
- **ARCHITECTURE PIVOT (per user, 2026-06-15)** — user: "DO NOT" rely on mock LLM; use the real OpenAI key and add a UI bring-your-own-key option. New stance: **real OpenAI is the default and drives every learner utterance, tutor turn, test answer, and judge (real calls).** A hidden BKT oracle governs whether learning happens + answer correctness, so the LLM gives authentic behavior while metrics stay reproducible — this *is* the credibility story. Deterministic templates demoted to an explicit `fallback` mode (API down only), never silent/default. Verified a real gpt-4o-mini call: the seeded persona produced the M1 misconception unprompted ("slope is 2, the number in front of x"). Key written to untracked `.env` (gitignored, confirmed). Per-request user key supported by API + UI.
